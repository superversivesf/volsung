# TTS Service Requirements
# Installed in Dockerfile.tts

# FastAPI framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.27.0

# PyTorch (modern version - 2.2+)
torch>=2.2.0
torchaudio>=2.2.0

# HuggingFace ecosystem
# NOTE: styletts2 requires huggingface-hub<0.20.0, transformers<4.40
transformers>=4.38.0,<4.40.0
huggingface-hub>=0.19.0,<0.20.0
accelerate>=0.30.0

# TTS models
# NOTE: Skipping qwen-tts due to dependency conflicts with styletts2
# qwen-tts requires transformers==4.57.3 which needs huggingface-hub>=0.34
# styletts2 requires huggingface-hub<0.20
# Only one can be used, using styletts2 for now
# qwen-tts
styletts2>=0.1.6

# Audio processing
soundfile>=0.12.0

# Data validation
pydantic>=2.0.0
numpy<2.0  # torch 2.1.0 requires numpy 1.x
