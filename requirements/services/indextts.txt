# IndexTTS-2 Service Requirements

# Core FastAPI stack
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0

# PyTorch
torch>=2.2.0
torchaudio>=2.0.0

# HuggingFace for model downloads
huggingface-hub[cli,hf_xet]>=0.20.0

# NOTE: IndexTTS-2 is not pip-installable.
# It is installed from source in the Dockerfile via:
#   git clone https://github.com/index-tts/index-tts.git
#   pip install -e .
