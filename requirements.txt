# Coordinator/Gateway Requirements
# These are lightweight - NO ML libraries
# The coordinator only routes requests to microservices

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.27.0
pydantic>=2.0.0
numpy>=1.24.0

# NOTE: No torch, transformers, qwen-tts, etc.
# Those are in requirements/services/*.txt for individual services
