# SFX Service Dependencies (AudioLDM2)
# Standalone sound effects generation service using AudioLDM2

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# PyTorch (pinned for compatibility)
torch==2.1.0
torchaudio==2.1.0

# Diffusers for AudioLDM2 (pinned for torch 2.1.0 compatibility)
diffusers==0.24.0
transformers>=4.35.0
accelerate>=0.25.0

# Audio processing
soundfile>=0.12.1
librosa>=0.10.1

# Data handling
numpy<2.0  # torch 2.1.0 requires numpy 1.x

# Optional: for better performance
# xformers  # Uncomment if CUDA available
