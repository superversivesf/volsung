# Music Service Dependencies (MusicGen)
# Standalone service for music generation using MusicGen
# Note: Uses Python 3.9 (required by audiocraft)

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# PyTorch (pinned for audiocraft compatibility)
torch==2.1.0
torchaudio==2.1.0

# Pin thinc for Python 3.9 compatibility
thinc<8.3.10

# Audio generation (audiocraft with MusicGen)
audiocraft>=1.0.0

# Audio processing
soundfile>=0.12.1
librosa>=0.10.0

# Data handling
numpy<2.0  # torch 2.1.0 requires numpy 1.x

# Optional: For better performance
# xformers  # Uncomment if using CUDA
